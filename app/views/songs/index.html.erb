<p id="notice"><%= notice %></p>

<h1>List Songs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<% @songs.each do |song| %>
  <p>
    <%= song.id %>
    <%= song.title %>
    <%= song.released %>
    <%= song.release_year %>
    <%= song.artist_name %>
    <%= song.genre %>
    <%= song.created_at%>
    <%= song.updated_at %>
  </p>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_song_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>